# Brewfile — source of truth for installed software.
# Install everything in here with:  brew bundle
# Add new packages as you install them: brew bundle dump --force (regenerates from
# current system) or just add the line manually and `brew install`.

# --- Taps ---
# tap "homebrew/cask-fonts"

# --- Formulae (CLI tools) ---
# brew "git"
# brew "wget"
brew "defaultbrowser"  # CLI tool to set the default browser
brew "jq"              # used by fresh-install.sh to patch Brave's prefs JSON

# --- Casks (GUI apps) ---
cask "brave-browser"
cask "clipy"  # clipboard manager — needs Accessibility permission granted manually, see SETUP-LOG.md
cask "keyboard-maestro"  # automation tool — paid license + permissions needed manually, see SETUP-LOG.md
cask "obsidian"
cask "visual-studio-code"
cask "claude"  # Anthropic's Claude desktop app (not claude-code, the CLI)
cask "iina"
cask "scroll-reverser"  # needs Accessibility + Input Monitoring permission, see SETUP-LOG.md
# cask "rectangle"
# cask "iterm2"
